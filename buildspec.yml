version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  build:
    commands:
      - echo Building the React Application...
      - npm run build
artifacts:
  files:
    - "**/*"
  base-directory: "dist"

